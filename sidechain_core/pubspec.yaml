name: sidechain_core
description: Headless sidechain runtime — generated protobuf, RPC clients, providers
version: 0.0.1
publish_to: none

environment:
  sdk: 3.12.2
  flutter: 3.44.8

dependencies:
  connectrpc: 1.0.0
  fixnum: ^1.1.1
  flutter:
    sdk: flutter
  protobuf: ^4.2.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
