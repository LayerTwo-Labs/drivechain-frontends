name: faucet
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # internal packages
  sail_ui:
    path: ../sail_ui

  cupertino_icons: ^1.0.6
  stacked: ^3.4.2
  get_it: ^7.6.7
  http: ^1.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
