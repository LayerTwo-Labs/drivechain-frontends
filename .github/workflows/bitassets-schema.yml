name: Bitassets schema snapshot

on:
  pull_request:
    paths:
      - "sidechain-orchestrator/sidechain/bitassets/**"
      - ".github/workflows/bitassets-schema.yml"

env:
  GO_VERSION: "1.25"

jobs:
  schema-validate:
    name: validate openapi_schema snapshot
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
      
      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: bitassets-schema-${{ env.GO_VERSION }}-${{ hashFiles('sidechain-orchestrator/go.sum') }}
          restore-keys: |
            bitassets-schema-${{ env.GO_VERSION }}-
      
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      # ---------------------------------------------------------------
      # Download the Bitassets release archive. It contains both the
      # server binary and the CLI binary. We use the CLI to generate
      # the OpenAPI schema locally (no mainchain node required).
      # ---------------------------------------------------------------
      - name: Cache Bitassets binary
        id: cache-bitassets
        uses: actions/cache@v4
        with:
          path: bitassets-cli
          key: bitassets-cli-latest
          
      - name: Download Bitassets binary
        if: steps.cache-bitassets.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          url="https://releases.drivechain.info/L2-S${SIDECHAIN_ID}-Bitassets-latest-x86_64-unknown-linux-gnu.zip"
          echo "Downloading $url"
          curl -fSL --retry 3 "$url" -o bitassets.zip
          unzip -o bitassets.zip
          # The CLI binary is named bitassets-cli-* after extraction
          CLI_BIN=$(ls bitassets-cli-* 2>/dev/null | head -1)
          if [ -z "$CLI_BIN" ]; then
            echo "::error::bitassets-cli binary not found in archive"
            ls -la
            exit 1
          fi
          chmod +x "$CLI_BIN"
          mv "$CLI_BIN" bitassets-cli
        env:
          SIDECHAIN_ID: "0"  # This would need to be set properly for each sidechain
          
      - name: Set CLI path
        run: |
          chmod +x bitassets-cli
          echo "BITASSETS_CLI=$PWD/bitassets-cli" >> "$GITHUB_ENV"

      - name: Generate schema from CLI and validate snapshot
        working-directory: sidechain-orchestrator
        run: |
          set -euo pipefail
          "$BITASSETS_CLI" openapi-schema > /tmp/live_schema.json
          # Validate it's real JSON
          python3 -m json.tool /tmp/live_schema.json > /dev/null
          echo "BITASSETS_CLI_SCHEMA=/tmp/live_schema.json" >> "$GITHUB_ENV"

      - name: Validate schema snapshot
        working-directory: sidechain-orchestrator
        env:
          BITASSETS_SCHEMA_VALIDATE_CLI: "1"
          BITASSETS_CLI_SCHEMA: /tmp/live_schema.json
        run: go test -v -run TestSchemaSnapshot ./sidechain/bitassets/
