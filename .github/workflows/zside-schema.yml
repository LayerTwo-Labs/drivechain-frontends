name: Zside schema snapshot

on:
  pull_request:
    paths:
      - "sidechain-orchestrator/sidechain/zside/**"
      - ".github/workflows/zside-schema.yml"

env:
  GO_VERSION: "1.25"

jobs:
  schema-validate:
    name: validate openapi_schema snapshot
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
      
      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: zside-schema-${{ env.GO_VERSION }}-${{ hashFiles('sidechain-orchestrator/go.sum') }}
          restore-keys: |
            zside-schema-${{ env.GO_VERSION }}-
      
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      # ---------------------------------------------------------------
      # Download the Zside release archive. It contains both the
      # server binary and the CLI binary. We use the CLI to generate
      # the OpenAPI schema locally (no mainchain node required).
      # ---------------------------------------------------------------
      - name: Cache Zside binary
        id: cache-zside
        uses: actions/cache@v4
        with:
          path: zside-cli
          key: zside-cli-latest
          
      - name: Download Zside binary
        if: steps.cache-zside.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          url="https://releases.drivechain.info/L2-S${SIDECHAIN_ID}-Zside-latest-x86_64-unknown-linux-gnu.zip"
          echo "Downloading $url"
          curl -fSL --retry 3 "$url" -o zside.zip
          unzip -o zside.zip
          # The CLI binary is named zside-cli-* after extraction
          CLI_BIN=$(ls zside-cli-* 2>/dev/null | head -1)
          if [ -z "$CLI_BIN" ]; then
            echo "::error::zside-cli binary not found in archive"
            ls -la
            exit 1
          fi
          chmod +x "$CLI_BIN"
          mv "$CLI_BIN" zside-cli
        env:
          SIDECHAIN_ID: "0"  # This would need to be set properly for each sidechain
          
      - name: Set CLI path
        run: |
          chmod +x zside-cli
          echo "ZSIDE_CLI=$PWD/zside-cli" >> "$GITHUB_ENV"

      - name: Generate schema from CLI and validate snapshot
        working-directory: sidechain-orchestrator
        run: |
          set -euo pipefail
          "$ZSIDE_CLI" openapi-schema > /tmp/live_schema.json
          # Validate it's real JSON
          python3 -m json.tool /tmp/live_schema.json > /dev/null
          echo "ZSIDE_CLI_SCHEMA=/tmp/live_schema.json" >> "$GITHUB_ENV"

      - name: Validate schema snapshot
        working-directory: sidechain-orchestrator
        env:
          ZSIDE_SCHEMA_VALIDATE_CLI: "1"
          ZSIDE_CLI_SCHEMA: /tmp/live_schema.json
        run: go test -v -run TestSchemaSnapshot ./sidechain/zside/
