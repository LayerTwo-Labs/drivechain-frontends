// CUSF mainchain mining service

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: cusf/mainchain/v1/mining.proto

package mainchainv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/LayerTwo-Labs/sidesail/sidechain-orchestrator/gen/cusf/mainchain/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MiningServiceName is the fully-qualified name of the MiningService service.
	MiningServiceName = "cusf.mainchain.v1.MiningService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MiningServiceGenerateToAddressProcedure is the fully-qualified name of the MiningService's
	// GenerateToAddress RPC.
	MiningServiceGenerateToAddressProcedure = "/cusf.mainchain.v1.MiningService/GenerateToAddress"
)

// MiningServiceClient is a client for the cusf.mainchain.v1.MiningService service.
type MiningServiceClient interface {
	// Mine blocks immediately to a specified address, returning the hashes of
	// the mined blocks. Analogous to Bitcoin Core's `generatetoaddress` RPC.
	//
	// The ACK policy for sidechain proposals is the persisted block producer
	// policy (see BlockProducerService.SetSidechainAck / SetAckAllProposals).
	//
	// On signet, blocks are produced by the signet miner, which sources its
	// template from the enforcer's own block template server. Signet therefore
	// requires the enforcer to run with `--enable-block-template-server`, and
	// only one block can be generated per call. The Bitcoin Core node's wallet
	// must also be able to solve the signet challenge.
	GenerateToAddress(context.Context, *connect.Request[v1.GenerateToAddressRequest]) (*connect.Response[v1.GenerateToAddressResponse], error)
}

// NewMiningServiceClient constructs a client for the cusf.mainchain.v1.MiningService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMiningServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MiningServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	miningServiceMethods := v1.File_cusf_mainchain_v1_mining_proto.Services().ByName("MiningService").Methods()
	return &miningServiceClient{
		generateToAddress: connect.NewClient[v1.GenerateToAddressRequest, v1.GenerateToAddressResponse](
			httpClient,
			baseURL+MiningServiceGenerateToAddressProcedure,
			connect.WithSchema(miningServiceMethods.ByName("GenerateToAddress")),
			connect.WithClientOptions(opts...),
		),
	}
}

// miningServiceClient implements MiningServiceClient.
type miningServiceClient struct {
	generateToAddress *connect.Client[v1.GenerateToAddressRequest, v1.GenerateToAddressResponse]
}

// GenerateToAddress calls cusf.mainchain.v1.MiningService.GenerateToAddress.
func (c *miningServiceClient) GenerateToAddress(ctx context.Context, req *connect.Request[v1.GenerateToAddressRequest]) (*connect.Response[v1.GenerateToAddressResponse], error) {
	return c.generateToAddress.CallUnary(ctx, req)
}

// MiningServiceHandler is an implementation of the cusf.mainchain.v1.MiningService service.
type MiningServiceHandler interface {
	// Mine blocks immediately to a specified address, returning the hashes of
	// the mined blocks. Analogous to Bitcoin Core's `generatetoaddress` RPC.
	//
	// The ACK policy for sidechain proposals is the persisted block producer
	// policy (see BlockProducerService.SetSidechainAck / SetAckAllProposals).
	//
	// On signet, blocks are produced by the signet miner, which sources its
	// template from the enforcer's own block template server. Signet therefore
	// requires the enforcer to run with `--enable-block-template-server`, and
	// only one block can be generated per call. The Bitcoin Core node's wallet
	// must also be able to solve the signet challenge.
	GenerateToAddress(context.Context, *connect.Request[v1.GenerateToAddressRequest]) (*connect.Response[v1.GenerateToAddressResponse], error)
}

// NewMiningServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMiningServiceHandler(svc MiningServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	miningServiceMethods := v1.File_cusf_mainchain_v1_mining_proto.Services().ByName("MiningService").Methods()
	miningServiceGenerateToAddressHandler := connect.NewUnaryHandler(
		MiningServiceGenerateToAddressProcedure,
		svc.GenerateToAddress,
		connect.WithSchema(miningServiceMethods.ByName("GenerateToAddress")),
		connect.WithHandlerOptions(opts...),
	)
	return "/cusf.mainchain.v1.MiningService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MiningServiceGenerateToAddressProcedure:
			miningServiceGenerateToAddressHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMiningServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedMiningServiceHandler struct{}

func (UnimplementedMiningServiceHandler) GenerateToAddress(context.Context, *connect.Request[v1.GenerateToAddressRequest]) (*connect.Response[v1.GenerateToAddressResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("cusf.mainchain.v1.MiningService.GenerateToAddress is not implemented"))
}
