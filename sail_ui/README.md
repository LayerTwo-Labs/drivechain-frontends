# sail_ui

Shared Flutter UI + provider layer for the drivechain apps (bitwindow, thunder, bitnames, bitassets, zside, truthcoin, photon, coinshift). Widgets, theme, RPC clients, and backend-state plumbing live here.

Changes here bump the patch version of every client app (see `scripts/bump-drivechain-versions`).
